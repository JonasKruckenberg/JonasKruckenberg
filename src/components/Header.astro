<div style="height: 15.5rem;" aria-hidden>
    <div class="stripes">
        <div></div>
        <div></div>
        <div class="spacer"></div>
        <div></div>
        <div class="spacer"></div>
        <div></div>
        <div class="spacer"></div>
        <div></div>
        <div class="spacer"></div>
        <div></div>
    </div>
</div>

<style lang="scss">
    @use "../styles/variables" as *;

    .stripes {
        margin: -1em;
        transform: translateY(-60%);
        position: absolute;
        width: 100vw;

        div {
            position: relative;
            height: 2.5rem;

            &::after {
                content: "";
                display: block;
                position: absolute;
                top: 100%;
                left: 0;
                width: 100%;
                height: calc(12 * 0.8rem);
                background: inherit;
                $mask: url("/mask.svg") 0% 100%/100% 100%;
                mask: $mask;
                -webkit-mask: $mask;
            }
        }

        .spacer {
            height: 0.8rem;
        }

        div:nth-of-type(1) {
            background: $black;
            z-index: 10;
        }
        div:nth-of-type(2) {
            background: $dark-red;
            z-index: 9;
        }
        div:nth-of-type(3) {
            background: $black;
            z-index: 8;
        }
        div:nth-of-type(4) {
            background: $red;
            z-index: 7;
        }
        div:nth-of-type(5) {
            background: $black;
            z-index: 6;
        }
        div:nth-of-type(6) {
            background: $orange;
            z-index: 5;
        }
        div:nth-of-type(7) {
            background: $black;
            z-index: 4;
        }
        div:nth-of-type(8) {
            background: $yellow;
            z-index: 3;
        }
        div:nth-of-type(9) {
            background: $black;
            z-index: 2;
        }
        div:nth-of-type(10) {
            background: $bright-yellow;
            z-index: 1;
        }
    }

    @media (max-width: 700px) {
        .stripes {
            transform: translateY(-35%);
        }
        .stripes div::after {
            height: calc(6 * 0.8rem);
        }
    }
</style>
